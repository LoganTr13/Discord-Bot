import Dice from "./Dice";
import MCServer from "./MCServer";
import Ping from "./Ping";
import Pokedex from "./Pokedex";
import Ship from "./Ship";

export const commands = [new Dice(), new Pokedex(), new Ping(), new Ship(), new MCServer()];
